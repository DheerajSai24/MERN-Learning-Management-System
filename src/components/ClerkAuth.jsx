import React from 'react';
import { SignIn, SignUp, UserButton, SignedIn, SignedOut, RedirectToSignIn } from '@clerk/clerk-react';

// Authentication routes and components for Clerk
// Use <SignIn /> and <SignUp /> for authentication pages
// Use <SignedIn> and <SignedOut> for conditional rendering
// Use <UserButton /> for user profile/actions

export { SignIn, SignUp, UserButton, SignedIn, SignedOut, RedirectToSignIn };
